/**
 * @file /src/defaultLogRoutes.mjs
 * @description Provides the default (empty) log route configuration. This file is
 * used by the build script when no user-defined configuration is found.
 */

export const logRoutes = [];