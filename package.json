{
  "name": "divortio-logdo",
  "version": "1.0.0",
  "description": "A zero-latency logging service for Cloudflare D1 using Workers & Durable Objects.",
  "private": true,
  "scripts": {
    "build:routes": "node build/buildRoutes.js",
    "dev": "npm run build:routes && wrangler dev",
    "deploy": "npm run build:routes && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "wrangler": "^4.31.0"
  },
  "main": "./src/worker.mjs"
}